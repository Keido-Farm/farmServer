const {dailyReport,weeklyReport} = require("../models")

class dailyReportController {
 static async postDailyReport (req,res,next){
  try {
    const {FarmId,mati,afkir,}
  } catch (err) {
    
  }
 }
}

module.exports = dailyReportController